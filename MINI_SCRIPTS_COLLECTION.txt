╔══════════════════════════════════════════════════════════════════╗
║             COLLECTION: MINI BATCH SCRIPTS                      ║
║          Các Script Nhỏ Có Thể Copy-Paste                      ║
╚══════════════════════════════════════════════════════════════════╝


⚠️ HƯỚNG DẪN SỬ DỤNG:
1. Copy đoạn code bên dưới
2. Mở Notepad (Windows)
3. Paste code
4. Save as: [Tên script].bat
5. Chạy as Administrator

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


## 📌 SCRIPT 1: QUICK TEMP CLEANUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Xóa nhanh %TEMP% folder (Nhanh nhất)
Giải phóng: ~500MB - 2GB
Thời gian: <1 phút

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul
setlocal enabledelayedexpansion

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    QUICK TEMP CLEANUP                         ║
echo ╚════════════════════════════════════════════════╝
echo.

whoami /priv | find "SeDebugPrivilege" >nul
if errorlevel 1 (
    echo Error: Need Admin rights!
    pause
    exit /b 1
)

echo Xóa %TEMP%...
for /d %%A in ("%temp%\*") do rd /s /q "%%A" 2>nul
del /q "%temp%\*.*" 2>nul
echo ✓ Cleaned

echo Xóa Windows Temp...
for /d %%A in ("%windir%\Temp\*") do rd /s /q "%%A" 2>nul
del /q "%windir%\Temp\*.*" 2>nul
echo ✓ Cleaned

echo.
echo Hoàn tất! Giải phóng ~500MB - 2GB
pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 2: BROWSER CACHE CLEANUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Xóa cache Chrome, Edge, Firefox
Giải phóng: ~1GB - 3GB
Thời gian: <2 phút

⚠️ LƯU Ý: Đóng tất cả browser trước!

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    BROWSER CACHE CLEANUP                      ║
echo ╚════════════════════════════════════════════════╝
echo.

whoami /priv | find "SeDebugPrivilege" >nul
if errorlevel 1 (
    echo Error: Need Admin rights!
    pause
    exit /b 1
)

echo Xóa Chrome cache...
for /d %%A in ("%APPDATA%\Google\Chrome\User Data\*") do (
    rd /s /q "%%A\Cache" 2>nul
    rd /s /q "%%A\Cache2" 2>nul
    rd /s /q "%%A\Code Cache" 2>nul
)
echo ✓ Chrome cleaned

echo Xóa Edge cache...
for /d %%A in ("%APPDATA%\Microsoft\Edge\User Data\*") do (
    rd /s /q "%%A\Cache" 2>nul
    rd /s /q "%%A\Cache2" 2>nul
    rd /s /q "%%A\Code Cache" 2>nul
)
echo ✓ Edge cleaned

echo Xóa Firefox cache...
for /d %%A in ("%APPDATA%\Mozilla\Firefox\Profiles\*") do (
    del /q "%%A\cache\*.*" 2>nul
    del /q "%%A\cache2\entries\*.*" 2>nul
)
echo ✓ Firefox cleaned

echo.
echo Hoàn tất! Giải phóng ~1GB - 3GB
pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 3: DEVELOPER TOOLS CLEANUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Xóa cache VSCode, npm, Python, Go
Giải phóng: ~1GB - 3GB
Thời gian: <2 phút

⚠️ LƯU Ý: Đóng VSCode trước!

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul
setlocal enabledelayedexpansion

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    DEVELOPER TOOLS CLEANUP                    ║
echo ╚════════════════════════════════════════════════╝
echo.

whoami /priv | find "SeDebugPrivilege" >nul
if errorlevel 1 (
    echo Error: Need Admin rights!
    pause
    exit /b 1
)

echo Xóa VSCode cache...
rd /s /q "%APPDATA%\Code\Cache" 2>nul
rd /s /q "%APPDATA%\Code\CachedData" 2>nul
rd /s /q "%APPDATA%\Code\GPUCache" 2>nul
echo ✓ VSCode cleaned

echo Xóa npm cache...
call npm cache clean --force 2>nul
rd /s /q "%APPDATA%\npm-cache" 2>nul
echo ✓ npm cleaned

echo Xóa Python cache...
for /r "%USERPROFILE%" %%F in (__pycache__) do (
    rd /s /q "%%F" 2>nul
)
echo ✓ Python cleaned

echo Xóa Go cache...
go clean -cache -modcache -testcache 2>nul
echo ✓ Go cleaned

echo.
echo Hoàn tất! Giải phóng ~1GB - 3GB
pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 4: SYSTEM OPTIMIZATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Xóa Prefetch, Crash Dumps, Recent Files
Giải phóng: ~500MB - 1GB
Thời gian: <1 phút

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    SYSTEM OPTIMIZATION CLEANUP                ║
echo ╚════════════════════════════════════════════════╝
echo.

whoami /priv | find "SeDebugPrivilege" >nul
if errorlevel 1 (
    echo Error: Need Admin rights!
    pause
    exit /b 1
)

echo Xóa Prefetch...
del /q "%windir%\Prefetch\*.*" 2>nul
echo ✓ Prefetch cleaned

echo Xóa CrashDumps...
for /d %%A in ("%windir%\CrashDumps\*") do rd /s /q "%%A" 2>nul
del /q "%windir%\CrashDumps\*.*" 2>nul
echo ✓ CrashDumps cleaned

echo Xóa Recent files...
rd /s /q "%APPDATA%\Microsoft\Windows\Recent" 2>nul
echo ✓ Recent cleaned

echo Xóa Thumbnails...
del /q "%windir%\explorer.exe.shellstate" 2>nul
echo ✓ Thumbnails cleaned

echo.
echo Hoàn tất! Giải phóng ~500MB - 1GB
pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 5: WINDOWS UPDATE CLEANUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Xóa Windows Update cache
Giải phóng: ~500MB - 2GB
Thời gian: <1 phút

⚠️ LƯU Ý: Không xóa nếu đang update Windows!

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    WINDOWS UPDATE CLEANUP                     ║
echo ╚════════════════════════════════════════════════╝
echo.

whoami /priv | find "SeDebugPrivilege" >nul
if errorlevel 1 (
    echo Error: Need Admin rights!
    pause
    exit /b 1
)

echo Dừng Windows Update...
net stop wuauserv 2>nul
timeout /t 1 /nobreak

echo Xóa cache...
rd /s /q "%windir%\SoftwareDistribution\Download" 2>nul
md "%windir%\SoftwareDistribution\Download" 2>nul

echo Khởi động Windows Update...
net start wuauserv 2>nul
timeout /t 2 /nobreak

echo.
echo Hoàn tất! Giải phóng ~500MB - 2GB
pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 6: RECYCLE BIN CLEANUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Xóa Recycle Bin hoàn toàn
Giải phóng: Tùy thuộc
Thời gian: <1 phút

⚠️ CẢNH BÁO: Không thể khôi phục!

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    RECYCLE BIN CLEANUP                        ║
echo ╚════════════════════════════════════════════════╝
echo.

whoami /priv | find "SeDebugPrivilege" >nul
if errorlevel 1 (
    echo Error: Need Admin rights!
    pause
    exit /b 1
)

echo WARNING: Recycle Bin sẽ bị xóa vĩnh viễn!
echo Nhấn Ctrl+C để hủy, hoặc nhấn phím để tiếp tục...
pause

echo Xóa Recycle Bin...
rd /s /q "C:\$Recycle.Bin" 2>nul
timeout /t 1 /nobreak

echo.
echo Hoàn tất! Recycle Bin đã xóa
pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 7: DISK SPACE ANALYZER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Kiểm tra kích thước folder chiếm nhiều dung lượng
Không cần Admin
Thời gian: ~30 giây

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul

cls
echo.
echo ╔════════════════════════════════════════════════╗
echo ║    DISK SPACE ANALYZER                        ║
echo ╚════════════════════════════════════════════════╝
echo.

setlocal enabledelayedexpansion

echo Kiểm tra các thư mục lớn...
echo.

REM AppData
for /F %%A in ('dir "%APPDATA%" /s /b 2^>nul ^| find /c /v ""') do (
    echo ✓ AppData: %%A items
)

REM Temp
for /F %%A in ('dir "%temp%" /s /b 2^>nul ^| find /c /v ""') do (
    echo ✓ Temp: %%A items
)

REM Windows
for /F %%A in ('dir "%windir%" /s /b 2^>nul ^| find /c /v ""') do (
    echo ✓ Windows: %%A items
)

REM Downloads
for /F %%A in ('dir "%USERPROFILE%\Downloads" /s /b 2^>nul ^| find /c /v ""') do (
    echo ✓ Downloads: %%A items
)

echo.
echo Hoàn tất!
pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 8: DISABLE UNNECESSARY SERVICES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Tắt các services không cần thiết
Giải phóng: ~10-20% CPU khi idle
Thời gian: <1 phút

⚠️ LƯU Ý: Chỉ tắt nếu bạn chắc chắn không cần!

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    DISABLE UNNECESSARY SERVICES              ║
echo ╚════════════════════════════════════════════════╝
echo.

whoami /priv | find "SeDebugPrivilege" >nul
if errorlevel 1 (
    echo Error: Need Admin rights!
    pause
    exit /b 1
)

echo WARNING: Chỉ tắt các services bạn không cần!
echo Các services này sẽ bị tắt:
echo - DiagTrack (Telemetry)
echo - dmwappushservice (Ads)
echo - OneSyncSvc (OneDrive Sync)
echo.
echo Nhấn Ctrl+C để hủy, hoặc nhấn phím để tiếp tục...
pause

echo Tắt DiagTrack...
net stop DiagTrack 2>nul
sc config DiagTrack start= disabled 2>nul
echo ✓ DiagTrack disabled

echo Tắt dmwappushservice...
net stop dmwappushservice 2>nul
sc config dmwappushservice start= disabled 2>nul
echo ✓ dmwappushservice disabled

echo Tắt OneSyncSvc...
net stop OneSyncSvc 2>nul
sc config OneSyncSvc start= disabled 2>nul
echo ✓ OneSyncSvc disabled

echo.
echo Hoàn tất! Services đã được disable
echo.
echo Để enable lại, chạy: sc config [servicename] start= auto
pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 9: DISPLAY FOLDER SIZES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Hiển thị kích thước các folder chính
Không cần Admin
Thời gian: ~2 phút (tùy HDD speed)

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul

cls
echo.
echo ╔════════════════════════════════════════════════╗
echo ║    FOLDER SIZE ANALYZER                       ║
echo ╚════════════════════════════════════════════════╝
echo.

echo Kiểm tra dung lượng các thư mục...
echo (Điều này có thể mất vài phút)
echo.

REM Sử dụng dir /s để tính dung lượng
for /d %%A in (
    "%TEMP%"
    "%APPDATA%\Google\Chrome"
    "%APPDATA%\Microsoft\Edge"
    "%windir%"
    "%USERPROFILE%\Downloads"
    "%USERPROFILE%\AppData\Local"
) do (
    if exist "%%A" (
        echo Checking: %%A
    )
)

echo.
echo Note: Sử dụng WizTree (free) hoặc TreeSize Pro để kết quả chi tiết
pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 10: FULL SYSTEM CLEANUP + DEFRAG
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Chạy Deep Clean + Defragmentation
Giải phóng: 2GB - 10GB
Thời gian: ~30-60 phút

⚠️ LƯU Ý: Chỉ dùng cho HDD, không dùng SSD!

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    FULL CLEANUP + DEFRAG                      ║
echo ║    Thời gian: 30-60 phút                     ║
echo ╚════════════════════════════════════════════════╝
echo.

whoami /priv | find "SeDebugPrivilege" >nul
if errorlevel 1 (
    echo Error: Need Admin rights!
    pause
    exit /b 1
)

echo WARNING: Tiến trình này sẽ mất 30-60 phút!
echo Nhấn Ctrl+C để hủy, hoặc nhấn phím để tiếp tục...
pause

echo [STEP 1/2] Bắt đầu Deep Cleanup...
REM Giả sử Deep_Clean_Windows_Advanced.bat nằm cùng thư mục
call Deep_Clean_Windows_Advanced.bat silent
echo ✓ Cleanup completed

echo.
echo [STEP 2/2] Bắt đầu Defragmentation...
echo ⏳ Điều này sẽ mất 15-45 phút...
defrag C: /O

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    HOÀN TẤT!                                  ║
echo ║    Máy tính sẽ nhanh hơn!                    ║
echo ╚════════════════════════════════════════════════╝
echo.

pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 11: REPAIR WINDOWS SYSTEM FILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Kiểm tra và sửa lỗi file hệ thống Windows
Thời gian: ~15-30 phút
Cần restart: Có thể

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    REPAIR WINDOWS SYSTEM FILES                ║
echo ╚════════════════════════════════════════════════╝
echo.

whoami /priv | find "SeDebugPrivilege" >nul
if errorlevel 1 (
    echo Error: Need Admin rights!
    pause
    exit /b 1
)

echo Tiến trình này sẽ mất 15-30 phút...
echo.

echo [1/2] System File Checker...
sfc /scannow
if errorlevel 1 (
    echo Lỗi được tìm thấy. Sử dụng:
    echo  sfc /restorehealth
    pause
)

echo.
echo [2/2] DISM Scan...
DISM /Online /Cleanup-Image /ScanHealth
DISM /Online /Cleanup-Image /RestoreHealth

echo.
echo Hoàn tất! Có thể cần khởi động lại Windows
pause

─────────────────────────────────────────────────────────────────


## 📌 SCRIPT 12: BACKUP REGISTRY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Tạo backup Registry
Không cần Admin
Thời gian: <1 phút

─────────────────────────────────────────────────────────────────

@echo off
chcp 65001 >nul

echo.
echo ╔════════════════════════════════════════════════╗
echo ║    REGISTRY BACKUP                            ║
echo ╚════════════════════════════════════════════════╝
echo.

set BACKUP_DIR=%USERPROFILE%\Desktop\RegBackup
if not exist "%BACKUP_DIR%" mkdir "%BACKUP_DIR%"

echo Tạo backup Registry...
echo Vị trí: %BACKUP_DIR%
echo.

for /f "tokens=2-4 delims=/ " %%a in ('date /t') do (
    set month=%%a
    set day=%%b
    set year=%%c
)

for /f "tokens=1-2 delims=/:" %%a in ('time /t') do (
    set hour=%%a
    set minute=%%b
)

set FILENAME=RegBackup_%year%-%month%-%day%_%hour%-%minute%.reg

echo Backup file: %FILENAME%

reg export HKEY_LOCAL_MACHINE "%BACKUP_DIR%\%FILENAME%"
if errorlevel 1 (
    echo Error tạo backup!
    pause
    exit /b 1
)

echo ✓ Registry backup created successfully!
echo Lưu tại: %BACKUP_DIR%
echo.
echo Để restore, double-click file .reg
pause

─────────────────────────────────────────────────────────────────

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 12 Scripts sẵn sàng!

Copy-paste bất kỳ script nào bạn muốn sử dụng!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
